# Vigil-Local-AI-Security-Camera
An end-to-end computer vision pipeline in pure C (no OpenCV or Python runtime): captures MJPEG webcam frames and runs  frame-differencing motion detection in ~3 ms on 1080p input (test hardware), persisting detected events to disk.
